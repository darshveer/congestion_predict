# Full toolchain for training, experiments, survival analysis and figure generation.
# Install with: pip install -r requirements-dev.txt
-r requirements.txt
scipy>=1.11
matplotlib>=3.7
lifelines>=0.27
lightgbm>=4.0   # needs OpenMP at runtime: `brew install libomp` (mac) / `libgomp1` (Debian)
xgboost>=2.0
